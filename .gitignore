# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual environment
venv/
env/
.venv/
.env/

# IDE / Editor specific
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
.DS_Store

# spaCy models (if downloaded locally within the project, though usually they are installed globally or in venv)
# Adjust if you have a specific directory for models
# en_core_web_sm/

# Output files
cate_blanchett_events.json
notified_events_urls.json
recipients.json
test.py
web_scraper_old.py

# Secrets/Credentials (if you move them to a file)
# config.ini
# .env
# credentials.json

# Yagmail specific (if it creates local cache or credential files)
yagmail.cache
# oauth2.json # Or whatever yagmail uses for OAuth credentials if stored locally
